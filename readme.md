# LLM customisation
Repo containing info for customising LLMs (currently only Ollama tested)
## How to create models
- add the modle detail to the list in the `models.json` file. 
- run `bash create_ollama_models.sh`